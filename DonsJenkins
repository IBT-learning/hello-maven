pipeline {
    agent any

    stages {
        stage('Git Clone') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Mvn Validate') {
                    steps {
                        bat 'mvn validate'
                    }
                }
    }
 }