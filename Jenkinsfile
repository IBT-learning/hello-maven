pipeline {
    agent any

    stages {
        stage('Git clone') {
            steps {
             echo "cloning or clowning"
             }
        }

        stage('Verify') {
            steps {
               sh 'dir'
            }
        }

        stage('Run mvn commands') {
            steps {
                echo "running maven commands.."
            }
            }
        }
    }